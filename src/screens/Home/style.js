import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },

  containerTitle: {
    alignItems: "center",
    marginTop: 10
  },

  containerPhotos: {
    
  }
});

export default styles;